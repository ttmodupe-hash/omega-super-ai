# Railway deployment configuration - OMEGA-LUQI engine.
# CRITICAL: the LAB SANDBOX image (sleep infinity) now lives at lab/Dockerfile,
# off the repo root, so Railway Dockerfile auto-detection cannot pick it up.
# This override keeps Railway building the engine, not the sleeping lab container.
[build]
builder = "NIXPACKS"          # python builder from requirements.txt

[deploy]
startCommand = "bash start.sh"
healthcheckPath = "/v1/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1               # single worker: task ledger is in-memory unless STATE_BACKEND=redis
