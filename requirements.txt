# ==============================================================================
# LUQI-AI MASTER PRODUCTION RUNTIME - DEPENDENCY MANIFEST
# Pinned for reproducible staging builds. Floors were previously >= ; the one
# conflict found in the pasted manifest (uvicorn 0.28 vs our 0.29 floor) is
# reconciled below. Unused deps (pydantic-settings, cryptography) removed.
# ==============================================================================

# Web server core
fastapi==0.110.0
uvicorn[standard]==0.29.0
httpx==0.27.0
websockets==12.0          # also bundled by uvicorn[standard], pinned explicitly

# Agent engines & orchestration
requests==2.31.0
pydantic==2.6.4

# Persistence (PostgreSQL default; MySQL via env DATABASE_URL dialect)
SQLAlchemy==2.0.28
psycopg2-binary==2.9.9
alembic==1.13.1
PyMySQL==1.1.0            # only required when DATABASE_URL uses mysql+pymysql

# Auth & session revocation (PyJWT is imported lazily - engine boots without it)
PyJWT==2.8.0
redis==5.0.3              # only required when STATE_BACKEND=redis

# Sandboxes
docker==7.0.0             # only required on nodes running lab terminals/compiles

# QA
pytest==8.1.1
pytest-asyncio==0.23.5    # reserved for future native-async tests (suites are sync TestClient)