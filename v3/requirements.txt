# Luqi-AI v3 — Omega AI Requirements
# Install: pip install -r requirements.txt

# Core dependencies (mostly stdlib, minimal external)
requests>=2.31.0

# Optional: for enhanced functionality
# python-dotenv>=1.0.0  # Fallback built-in if not installed
# openai>=1.0.0         # Only needed for OpenAI fallback
# colorama>=0.4.6       # Windows color support (optional)
